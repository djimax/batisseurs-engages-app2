import { useMemo, useState } from "react";
import { Building2, Check, ClipboardList, Download, FileText, Plus, Search, X } from "lucide-react";
import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Badge } from "@/components/ui/badge";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { Textarea } from "@/components/ui/textarea";
import { toast } from "sonner";

const statusLabels: Record<string, string> = { draft: "Brouillon", submitted: "Soumise", approved: "Approuvée", rejected: "Rejetée", ordered: "Commandée", received: "Réceptionnée", paid: "Payée", cancelled: "Annulée" };
const statusStyles: Record<string, string> = { draft: "bg-muted text-muted-foreground", submitted: "bg-amber-100 text-amber-800", approved: "bg-emerald-100 text-emerald-800", rejected: "bg-red-100 text-red-800", ordered: "bg-blue-100 text-blue-800", received: "bg-violet-100 text-violet-800", paid: "bg-primary/10 text-primary", cancelled: "bg-muted text-muted-foreground" };

export default function Purchases() {
  const utils = trpc.useUtils();
  const suppliers = trpc.purchases.suppliers.useQuery();
  const requests = trpc.purchases.requests.useQuery();
  const quotes = trpc.purchases.quotes.useQuery();
  const createSupplier = trpc.purchases.createSupplier.useMutation({ onSuccess: () => { toast.success("Fournisseur ajouté"); utils.purchases.suppliers.invalidate(); setSupplierForm({ name: "", email: "", phone: "", notes: "" }); }, onError: (error) => toast.error(error.message) });
  const createRequest = trpc.purchases.createRequest.useMutation({ onSuccess: () => { toast.success("Demande enregistrée"); utils.purchases.requests.invalidate(); setRequestForm({ description: "", category: "", amount: "", currency: "XOF", supplierId: "", justification: "" }); }, onError: (error) => toast.error(error.message) });
  const updateStatus = trpc.purchases.updateStatus.useMutation({ onSuccess: () => { toast.success("Statut mis à jour"); utils.purchases.requests.invalidate(); }, onError: (error) => toast.error(error.message) });
  const [supplierForm, setSupplierForm] = useState({ name: "", email: "", phone: "", notes: "" });
  const [requestForm, setRequestForm] = useState({ description: "", category: "", amount: "", currency: "XOF" as "EUR" | "XOF", supplierId: "", justification: "" });
  const [search, setSearch] = useState("");
  const filteredRequests = useMemo(() => (requests.data ?? []).filter((item) => `${item.description} ${item.category} ${item.supplierName ?? ""}`.toLowerCase().includes(search.toLowerCase())), [requests.data, search]);
  const totalCommitted = filteredRequests.filter((item) => ["approved", "ordered", "received", "paid"].includes(item.status)).reduce((sum, item) => sum + Number(item.amount), 0);
  const exportRequests = () => { const header = ["ID", "Description", "Catégorie", "Fournisseur", "Montant", "Devise", "Statut"]; const rows = filteredRequests.map((item) => [item.id, item.description, item.category, item.supplierName ?? "", item.amount, item.currency, statusLabels[item.status] ?? item.status]); const csv = [header, ...rows].map((row) => row.map((value) => `"${String(value).replaceAll('"', '""')}"`).join(",")).join("\\n"); const url = URL.createObjectURL(new Blob(["\\uFEFF" + csv], { type: "text/csv;charset=utf-8" })); const link = document.createElement("a"); link.href = url; link.download = "demandes-achats.csv"; link.click(); URL.revokeObjectURL(url); toast.success("Export CSV généré"); };

  return <div className="container space-y-6 py-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-medium text-primary">Gestion financière</p><h1 className="text-3xl font-bold tracking-tight">Achats et fournisseurs</h1><p className="mt-1 text-muted-foreground">Structurez les demandes, comparez les offres et gardez une trace des validations.</p></div><div className="grid grid-cols-2 gap-3 text-sm"><div className="rounded-xl border bg-card px-4 py-3"><p className="text-muted-foreground">Fournisseurs actifs</p><p className="text-xl font-semibold">{suppliers.data?.filter((item) => item.status === "active").length ?? 0}</p></div><div className="rounded-xl border bg-card px-4 py-3"><p className="text-muted-foreground">Engagé</p><p className="text-xl font-semibold">{totalCommitted.toLocaleString("fr-FR")} {requestForm.currency === "XOF" ? "F" : "€"}</p></div></div></div>
    <Tabs defaultValue="requests" className="space-y-4"><TabsList><TabsTrigger value="requests"><ClipboardList className="mr-2 h-4 w-4" />Demandes d’achat</TabsTrigger><TabsTrigger value="quotes"><FileText className="mr-2 h-4 w-4" />Devis</TabsTrigger><TabsTrigger value="suppliers"><Building2 className="mr-2 h-4 w-4" />Fournisseurs</TabsTrigger></TabsList>
      <TabsContent value="requests" className="space-y-4"><Card><CardHeader><div className="flex flex-wrap items-center justify-between gap-3"><div><CardTitle>Demandes d’achat</CardTitle><CardDescription>Les demandes soumises suivent un circuit de validation audité.</CardDescription></div><Button variant="outline" size="sm" onClick={exportRequests}><Download className="mr-2 h-4 w-4" />Exporter CSV</Button><div className="relative w-full sm:w-72"><Search className="absolute left-3 top-2.5 h-4 w-4 text-muted-foreground" /><Input value={search} onChange={(event) => setSearch(event.target.value)} className="pl-9" placeholder="Rechercher une demande" /></div></div></CardHeader><CardContent className="space-y-3">{filteredRequests.length === 0 ? <p className="rounded-xl border border-dashed p-6 text-center text-sm text-muted-foreground">Aucune demande enregistrée.</p> : filteredRequests.map((item) => <div key={item.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border p-4"><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><p className="font-medium">{item.description}</p><Badge className={statusStyles[item.status]}>{statusLabels[item.status] ?? item.status}</Badge></div><p className="mt-1 text-sm text-muted-foreground">{item.category} · {item.supplierName ?? "Fournisseur à définir"} · {Number(item.amount).toLocaleString("fr-FR")} {item.currency === "XOF" ? "F" : "€"}</p>{item.justification ? <p className="mt-2 text-sm text-muted-foreground">{item.justification}</p> : null}</div><div className="flex flex-wrap gap-2">{item.status === "submitted" ? <><Button size="sm" variant="outline" onClick={() => updateStatus.mutate({ id: item.id, status: "rejected" })}><X className="mr-1 h-4 w-4" />Rejeter</Button><Button size="sm" onClick={() => updateStatus.mutate({ id: item.id, status: "approved" })}><Check className="mr-1 h-4 w-4" />Approuver</Button></> : item.status === "approved" ? <Button size="sm" variant="outline" onClick={() => updateStatus.mutate({ id: item.id, status: "ordered" })}>Marquer commandée</Button> : null}</div></div>)}</CardContent></Card><Card><CardHeader><CardTitle>Nouvelle demande</CardTitle><CardDescription>Une demande en brouillon peut être soumise depuis cet espace.</CardDescription></CardHeader><CardContent><form className="grid gap-4 md:grid-cols-2" onSubmit={(event) => { event.preventDefault(); createRequest.mutate({ description: requestForm.description, category: requestForm.category, amount: requestForm.amount, currency: requestForm.currency, supplierId: requestForm.supplierId ? Number(requestForm.supplierId) : null, justification: requestForm.justification || null, status: "submitted" }); }}><div className="space-y-2"><Label>Description</Label><Input required value={requestForm.description} onChange={(e) => setRequestForm({ ...requestForm, description: e.target.value })} placeholder="Fournitures de chantier" /></div><div className="space-y-2"><Label>Catégorie</Label><Input required value={requestForm.category} onChange={(e) => setRequestForm({ ...requestForm, category: e.target.value })} placeholder="Matériel" /></div><div className="space-y-2"><Label>Montant</Label><Input required inputMode="decimal" value={requestForm.amount} onChange={(e) => setRequestForm({ ...requestForm, amount: e.target.value })} placeholder="250000" /></div><div className="space-y-2"><Label>Devise</Label><select className="flex h-10 w-full rounded-md border bg-background px-3 text-sm" value={requestForm.currency} onChange={(e) => setRequestForm({ ...requestForm, currency: e.target.value as "EUR" | "XOF" })}><option value="XOF">Franc CFA (F)</option><option value="EUR">Euro (€)</option></select></div><div className="space-y-2"><Label>Fournisseur</Label><select className="flex h-10 w-full rounded-md border bg-background px-3 text-sm" value={requestForm.supplierId} onChange={(e) => setRequestForm({ ...requestForm, supplierId: e.target.value })}><option value="">À définir</option>{(suppliers.data ?? []).filter((item) => item.status === "active").map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></div><div className="space-y-2 md:col-span-2"><Label>Justification</Label><Textarea value={requestForm.justification} onChange={(e) => setRequestForm({ ...requestForm, justification: e.target.value })} placeholder="Contexte, projet concerné ou urgence" /></div><div className="md:col-span-2"><Button type="submit" disabled={createRequest.isPending}><Plus className="mr-2 h-4 w-4" />Soumettre la demande</Button></div></form></CardContent></Card></TabsContent>
      <TabsContent value="quotes" className="space-y-4"><Card><CardHeader><CardTitle>Devis reçus</CardTitle><CardDescription>Les offres sont conservées avec leur montant et leur état de sélection.</CardDescription></CardHeader><CardContent className="space-y-3">{(quotes.data ?? []).length === 0 ? <p className="rounded-xl border border-dashed p-6 text-center text-sm text-muted-foreground">Aucun devis enregistré.</p> : (quotes.data ?? []).map((quote) => <div key={quote.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border p-4"><div><p className="font-medium">Devis {quote.quoteNumber ?? `#${quote.id}`}</p><p className="text-sm text-muted-foreground">Demande #{quote.purchaseRequestId} · {Number(quote.amount).toLocaleString("fr-FR")} {quote.currency === "XOF" ? "F" : "€"}</p></div><Badge variant={quote.status === "selected" ? "default" : "secondary"}>{quote.status === "selected" ? "Retenu" : quote.status === "rejected" ? "Écarté" : "À étudier"}</Badge></div>)}</CardContent></Card></TabsContent>
      <TabsContent value="suppliers" className="space-y-4"><Card><CardHeader><CardTitle>Répertoire fournisseurs</CardTitle><CardDescription>Coordonnées et statut des partenaires d’achat.</CardDescription></CardHeader><CardContent className="space-y-3">{(suppliers.data ?? []).map((item) => <div key={item.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border p-4"><div><p className="font-medium">{item.name}</p><p className="text-sm text-muted-foreground">{item.email ?? "E-mail non renseigné"}{item.phone ? ` · ${item.phone}` : ""}</p></div><Badge variant={item.status === "active" ? "default" : "secondary"}>{item.status === "active" ? "Actif" : "Inactif"}</Badge></div>)}</CardContent></Card><Card><CardHeader><CardTitle>Ajouter un fournisseur</CardTitle><CardDescription>Les coordonnées sont limitées aux informations opérationnelles nécessaires.</CardDescription></CardHeader><CardContent><form className="grid gap-4 md:grid-cols-2" onSubmit={(event) => { event.preventDefault(); createSupplier.mutate({ name: supplierForm.name, email: supplierForm.email || undefined, phone: supplierForm.phone || undefined, notes: supplierForm.notes || undefined }); }}><div className="space-y-2"><Label>Nom</Label><Input required value={supplierForm.name} onChange={(e) => setSupplierForm({ ...supplierForm, name: e.target.value })} /></div><div className="space-y-2"><Label>E-mail</Label><Input type="email" value={supplierForm.email} onChange={(e) => setSupplierForm({ ...supplierForm, email: e.target.value })} /></div><div className="space-y-2"><Label>Téléphone</Label><Input value={supplierForm.phone} onChange={(e) => setSupplierForm({ ...supplierForm, phone: e.target.value })} /></div><div className="space-y-2 md:col-span-2"><Label>Notes</Label><Textarea value={supplierForm.notes} onChange={(e) => setSupplierForm({ ...supplierForm, notes: e.target.value })} /></div><div><Button type="submit" disabled={createSupplier.isPending}><Plus className="mr-2 h-4 w-4" />Ajouter</Button></div></form></CardContent></Card></TabsContent>
    </Tabs></div>;
}
